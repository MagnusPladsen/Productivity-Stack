# ripgrep

> Recursively searches directories for a regex pattern — faster than grep, respects .gitignore.

**Platforms:** macOS, Linux, Windows

## Links
- [GitHub Repository](https://github.com/BurntSushi/ripgrep) ⭐ 60.9k

## Install

### macOS

```bash
brew install ripgrep
```

### Linux

**Debian/Ubuntu:**

```bash
sudo apt install ripgrep
```

**Arch Linux:**

```bash
sudo pacman -S ripgrep
```

**Fedora:**

```bash
sudo dnf install ripgrep
```

### Windows

```bash
winget install BurntSushi.ripgrep.MSVC
```

Or via Scoop:

```bash
scoop install ripgrep
```

Or via Chocolatey:

```bash
choco install ripgrep
```

## Alternatives
- [fd.md](fd.md) — fast find alternative by the same author (sharkdp), often used alongside ripgrep
